# مشروع حلقتي — دليل المشروع والميزات

هذا الملف يصف **النسخة الحالية** من مستودع «حلقتي»: الخلفية (Laravel + Filament) وتطبيق الجوال (Flutter)، مع **كل ميزة على حدة**، و**الإضافات** التي تم تنفيذها لدعم أدوار متعددة في التطبيق.

---

## 1. نظرة عامة

**حلقتي** منظومة لإدارة الحلقات القرآنية والطلاب والمعلمين والاختبارات والإشراف. تتكوّن من:

| الطبقة | التقنية | الوظيفة |
|--------|---------|---------|
| **لوحة إدارة ويب** | Laravel + Filament | إدارة كاملة للبيانات (CRUD)، تقارير، لوحات KPI |
| **واجهة برمجة للجوال** | Laravel Sanctum + `routes/api.php` | تطبيق Flutter: تسجيل دخول، بيانات حسب الدور |
| **تطبيق الجوال** | Flutter (Dart) | واجهة عربية RTL للمعلم، المختبر، ومشرف الحلقات |

**أدوار مسموحة في تطبيق الجوال** (بترتيب الأولوية عند الربط بحساب واحد يحمل أكثر من دور): `Teacher`، ثم `Examiner`، ثم `CenterSupervisor`. يُحدَّد الدور في الخادم عبر الصنف `App\Support\MobileAppRole`.

---

## 2. هيكل المستودع

```
halqati/
├── backend/          # Laravel — API + Filament + قاعدة البيانات
└── mobile/           # Flutter — تطبيق «حلقتي»
```

---

## 3. الخلفية (Laravel)

### 3.1 التقنيات الأساسية

- **Laravel** مع **Sanctum** لتوكنات API (`mobile-app`).
- **Spatie Laravel Permission** للأدوار والصلاحيات (`RolesAndPermissionsSeeder`).
- **Filament v3** لوحة إدارة على المسار `/admin`.
- **Kreait Laravel Firebase** لإرسال إشعارات **FCM** (عند توفر بيئة الإنتاج وملف الاعتماد).
- **Maatwebsite Excel** لتصدير التقارير بصيغة XLSX.
- **Spatie Laravel Activitylog** لتسجيل تغييرات محددة على النماذج (مع صفحة عرض في Filament لـ SuperAdmin).

### 3.2 المصادقة وواجهة الجوال

| الميزة | الوصف |
|--------|--------|
| **تسجيل الدخول** | `POST /api/login` — يتحقق من البريد وكلمة المرور، تفعيل الحساب، ثم يتحقق أن المستخدم يملك أحد الأدوار الثلاثة المعتمدة للجوال عبر `MobileAppRole::resolve()`. يقبل حقلاً اختيارياً **`fcm_token`** (حتى 500 حرف) ويُحفظ على المستخدم بعد نجاح المصادقة. |
| **تحديث رمز FCM** | `POST /api/fcm-token` (مع `auth:sanctum`) — جسم `{ "fcm_token": "..." }` لتحديث رمز الإشعارات بعد تسجيل الدخول أو عند تجديد الرمز. |
| **إشعارات داخل التطبيق** | مسارات تحت Sanctum: `GET /api/notifications` (صفحات)، `GET /api/notifications/unread-count`، `POST /api/notifications/{id}/read`، `POST /api/notifications/read-all`. تُخزَّن النسخ في جدول **`app_notifications`** عند إرسال إشعار لمستخدم (إلى جانب FCM عند التفعيل). |
| **رفض الحسابات غير المصرَّح بها** | إذا لم يكن للمستخدم أحد الأدوار: Teacher / Examiner / CenterSupervisor، يُرجع خطأ تحقق برسالة عربية. |
| **الجلسة بعد إعادة التشغيل** | `GET /api/session` (مع `Authorization: Bearer`) — يعيد `{ user: { id, name, email, role } }` لاستعادة الدور عند وجود توكن بدون تخزين محلي للدور. |
| **تسجيل الخروج** | `POST /api/logout` — حذف التوكن الحالي. |

### 3.3 مسارات المعلم (API) — `role:Teacher`

| المسار | الوظيفة |
|--------|---------|
| `GET /api/teacher/me` | بيانات المعلم والحلقة والصلاحيات (مثل تعديل صورة الملف الشخصي). |
| `PUT` / `POST /api/teacher/profile` | تحديث ملف المعلم (يشمل رفع صورة عبر POST لدعم multipart). |
| `GET /api/teacher/students` | قائمة طلاب حلقة المعلم. |
| `GET /api/teacher/students/{id}` | تفاصيل طالب. |
| `GET` / `PUT` / `POST /api/teacher/students/{id}/profile` | عرض/تحديث ملف الطالب؛ POST للرفع مع صورة. |
| `POST /api/teacher/students/{id}/profile/submit` | إرسال الملف للمراجعة (حسب منطق النظام). |
| `GET /api/teacher/halaqah/today` | يوم اليوم: طلاب، حضور، تقييم يومي، حفظ/مراجعة، أسباب التقييم. |
| `POST /api/teacher/daily-records/upsert` | حفظ أو تحديث سجلات اليوم (حضور، تقييم، حفظ). |
| `GET /api/teacher/reports/monthly` | تقرير شهري للحلقة. |

### 3.4 مسارات المختبر (API) — `role:Examiner`

النطاق يُطبَّق عبر السياسات `TestPolicy`، `TestAssignmentPolicy`، `TestResultPolicy` (استعلامات مفلترة حسب مراكز المستخدم).

| المسار | الوظيفة |
|--------|---------|
| `GET /api/examiner/me` | هوية المستخدم، دور Examiner، معرفات المراكز الإدارية. |
| `GET /api/examiner/stats` | إحصاءات مختصرة للوحة المختبر. |
| `GET /api/examiner/daily-summary` | ملخص يومي ضمن النطاق. |
| `GET /api/examiner/students/{student}` | تفاصيل طالب للمختبر. |
| `POST /api/examiner/test-results` | تسجيل/تحديث نتيجة اختبار؛ بعد الحفظ يُرسل إشعار FCM + تسجيل داخلي للمعلم المرتبط بالحلقة (عند التفعيل). |
| `GET /api/examiner/tests` | قائمة اختبارات ضمن النطاق. |
| `GET /api/examiner/tests/{test}/assignments` | تعيينات اختبار محدد. |
| `GET /api/examiner/test-assignments` | تعيينات الاختبارات (طلبات، ربط طلاب، حالة، وجود نتيجة). |
| `GET /api/examiner/test-results` | نتائج الاختبارات (مستوى، درجة، تاريخ). |

> **ملاحظة:** إنشاء الاختبارات والتعيينات الأساسية يبقى عادةً من **لوحة Filament**؛ الجوال يركّز على سير عمل التسجيل الميداني للنتائج.

### 3.5 مسارات مشرف الحلقات (API) — `role:CenterSupervisor`

| المسار | الوظيفة |
|--------|---------|
| `GET /api/supervisor/me` | هوية المستخدم ودور CenterSupervisor وعدد المراكز. |
| `GET /api/supervisor/stats` | إحصاءات للوحة المشرف. |
| `GET /api/supervisor/centers` | مراكز ضمن نطاق `UserScope`. |
| `GET /api/supervisor/halaqahs` | حلقات ضمن النطاق. |
| `GET /api/supervisor/teachers` | ملفات المعلمين المرتبطة بحلقات تلك المراكز. |
| `GET /api/supervisor/teachers/{teacher}` | تفاصيل معلم. |
| `GET /api/supervisor/halaqahs/{halaqah}/daily` | السجل اليومي لحلقة (للمشرف). |
| `POST /api/supervisor/visits` | تسجيل زيارة ميدانية؛ بعد الحفظ يُرسل إشعار للمعلم (FCM + سجل داخلي عند التفعيل). |
| `GET /api/supervisor/visits` | زيارات المشرف. |
| `GET /api/supervisor/attendance-stats` | إحصاءات حضور ضمن النطاق. |

### 3.6 خدمة الصور (CORS للويب)

| المسار | الوظيفة |
|--------|---------|
| `GET /api/student-photo?path=...` | بث ملف من `storage/public` تحت المسارات المسموحة (`student-photos/`, `teacher-photos/`) لتعمل `Image.network` في Flutter Web دون مشاكل نطاق مختلف. |

### 3.7 الإشعارات (FCM) والجدولة والخدمات

| العنصر | الوصف |
|--------|--------|
| **إعداد Firebase** | ملف JSON لحساب الخدمة في `storage/app/firebase-credentials.json` (أو مسار عبر `.env`: `FIREBASE_CREDENTIALS`). **لا يُرفع الملف إلى Git** (مدرج في `.gitignore`). في `config/firebase.php` يُستخدم `env('FIREBASE_CREDENTIALS', storage_path('app/firebase-credentials.json'))`. |
| **`NotificationService`** | `app/Services/NotificationService.php` — إرسال عبر Kreait عند **الإنتاج** ووجود ملف الاعتماد؛ حفظ صف في **`AppNotification`** لكل إرسال ناجح؛ مسح `fcm_token` عند أخطاء تسجيل الرمز المعروفة. |
| **فئات الإشعارات** | `HalaqahNotRecordedNotification` (جدولة)، `SupervisoryVisitNotification` (بعد `storeVisit`)، `ExamResultNotification` (بعد `storeResult`) — نصوص عربية وبيانات `type` للتوجيه من الجوال. |
| **أمر مجدول** | `php artisan notify:unrecorded-halaqahs` — تذكير للمعلمين ومشرفي المراكز عند حلقات نشطة منتهية الوقت بلا سجل حضور لليوم. مُسجَّل في `routes/console.php` يومياً **21:00** بتوقيت `Asia/Riyadh` مع `withoutOverlapping()`. |
| **تنظيف سجل النشاط** | `activitylog:clean --days=90` شهرياً في نفس الملف. |
| **خادم الإنتاج** | يتطلب سطر cron: `* * * * * cd /path-to-backend && php artisan schedule:run`. |

---

## 4. لوحة الإدارة Filament (`/admin`)

لوحة عربية (خط Tajawal، واجهة RTL عبر middleware مخصص). تتضمن على سبيل المثال لا الحصر:

| المورد / الصفحة | الغرض |
|-----------------|--------|
| **Regions** | المناطق |
| **Centers** | المراكز |
| **Halaqahs** | الحلقات |
| **Users** | المستخدمون والأدوار |
| **Teacher Profiles** | ملفات المعلمين |
| **Students** + **Enrollments** | الطلاب والتسجيل في الحلقات |
| **Tests** + **Test Assignments** + **Test Results** | الاختبارات والتعيينات والنتائج |
| **Supervision Rubrics** + **Supervisory Visits** | زيارات الإشراف والدرجات |
| **Student Profile Submissions** | طلبات/مراجعة ملفات الطلاب |
| **Admin Dashboard** + **Widgets** | إحصائيات ومؤشرات، بما فيها ويدجتات إضافية: متوسط نتائج الاختبارات (آخر 6 أشهر)، أقل الحلقات حضوراً (7 أيام)، مقارنة درجات الزيارات الإشرافية (6 أشهر)، جدول أكثر المعلمين تسجيلاً لغياب (الشهر الحالي). |
| **صفحات تقارير** | تقارير حضور، إشراف، اختبارات (`Filament/Pages/Reports`) — مع **تصدير CSV** كما هو سابقاً و**تصدير Excel** (Maatwebsite) من تقرير الحضور وتقرير الاختبارات. |
| **سجل الأحداث** | صفحة **«سجل الأحداث»** (`ActivityLogPage`) ضمن مجموعة «النظام» — مرئية لـ **SuperAdmin** فقط؛ تعرض سجل Spatie Activity Log مع فلتر بالمستخدم. |

الإدارة الكاملة للبيانات تتم من هنا؛ تطبيق الجوال يعتمد على مسارات API (بما فيها تسجيل النتائج والزيارات للمختبر والمشرف حيث وُجدت).

---

## 5. تطبيق Flutter (`mobile/`)

### 5.1 الإعدادات العامة

- لغة واجهة: **عربي**، اتجاه **RTL**.
- الاتصال: `ApiConstants.baseUrl` يختار تلقائياً `127.0.0.1` للويب وسطح المكتب، و`10.0.2.2` لمحاكي Android، مع إمكانية تعديل عنوان الشبكة للهاتف الحقيقي.
- التخزين الآمن: **flutter_secure_storage** للتوكن ولـ **دور المستخدم** (`saveRole` / `getRole`) ولـ **رمز FCM** (`saveFcmToken` / `getFcmToken` / مسح عند الخروج).
- **Firebase:** `firebase_core`، `firebase_messaging`، `flutter_local_notifications` — تهيئة في `main` (مع `firebase_options.dart`؛ يُفضَّل تشغيل `flutterfire configure` لبيئة الإنتاج). طلب أذونات الإشعارات، الاستماع لتجديد الرمز ومزامنته مع الخادم عند وجود جلسة.

### 5.2 التشغيل البارد (Splash)

عند فتح التطبيق:

1. إن لم يوجد توكن → شاشة تسجيل الدخول.
2. إن وُجد توكن **دون** دور محفوظ → استدعاء `GET /session` لاستعادة الدور وحفظه.
3. إن فشل الجلسة → مسح التوكن والعودة لتسجيل الدخول.
4. إن وُجد توكن ودور → الانتقال إلى `RoleHome` بالدور المناسب.

### 5.3 التوجيه حسب الدور — `lib/app/role_home.dart`

| الدور (`AppRole`) | الشاشة الرئيسية |
|-------------------|-----------------|
| `Teacher` | `HomePage` — لوحة المعلم الكاملة |
| `Examiner` | `ExaminerHomePage` — لوحة المختبر + قوائم |
| `CenterSupervisor` | `SupervisorHomePage` — لوحة مشرف الحلقات + قوائم |
| غير معروف | العودة إلى `LoginPage` |

### 5.4 ميزات واجهة المعلم (لكل ميزة على حدة)

| الميزة | الوصف |
|--------|--------|
| **لوحة الرئيسية** | عرض ملخص الملف، روابط للطلاب، السجل اليومي، التقرير الشهري، **زر جرس الإشعارات**، تسجيل خروج. |
| **الملف الشخصي للمعلم** | جلب `teacher/me`؛ رفع صورة شخصية عند السماح بذلك (`can_edit_own_photo`). |
| **قائمة الطلاب** | استعراض طلاب الحلقة والانتقال لتفاصيل/ملف الطالب. |
| **ملف الطالب** | عرض وتعديل حقول الملف، رفع صورة، إرسال للمراجعة عند توفر المسارات. |
| **السجل اليومي** | شاشة يومية: حضور، تقييم (مع أسباب)، حفظ/مراجعة؛ حفظ عبر `daily-records/upsert`. |
| **التقرير الشهري** | رسوم بيانية وجداول من `reports/monthly`. |

### 5.5 ميزات واجهة المختبر

| الميزة | الوصف |
|--------|--------|
| **لوحة المختبر** | إحصاءات وملخص يومي وروابط لسير العمل؛ **زر جرس الإشعارات** يفتح شاشة الإشعارات. |
| **الاختبارات** | قائمة من `GET /examiner/tests` وتعيينات لكل اختبار. |
| **تعيينات الاختبارات** | قائمة من `GET /examiner/test-assignments`. |
| **نتائج الاختبارات** | عرض وتسجيل نتائج عبر `POST /examiner/test-results` حسب تدفق الواجهة. |
| **الإشعارات** | صفحة مشتركة (`NotificationsPage`) تستدعي API الإشعارات؛ شارة بعدد غير المقروء على أيقونة الجرس. |

### 5.6 ميزات واجهة مشرف الحلقات

| الميزة | الوصف |
|--------|--------|
| **لوحة مشرف الحلقات** | إحصاءات وروابط؛ **زر جرس الإشعارات**. |
| **المراكز** | قائمة من `GET /supervisor/centers`. |
| **الحلقات** | قائمة من `GET /supervisor/halaqahs` مع إمكانية عرض السجل اليومي للحلقة. |
| **المعلمون** | قائمة من `GET /supervisor/teachers` وتفاصيل معلم. |
| **الزيارات** | تسجيل زيارة ميدانية عبر `POST /supervisor/visits` وعرض الزيارات. |
| **الإشعارات** | نفس صفحة الإشعارات المشتركة وشارة غير المقروء. |

### 5.7 المصادقة في التطبيق — `AuthRepository`

- عند **تسجيل الدخول الناجح**: حفظ التوكن **والدور** في التخزين الآمن؛ إن وُجد `fcm_token` محفوظ يُرسل مع تسجيل الدخول أو يُحدَّث لاحقاً عبر `POST /api/fcm-token`.
- عند **تسجيل الخروج**: استدعاء `/logout` ثم `deleteToken()` الذي يمسح التوكن والدور و**رمز FCM** المحلي.
- **`fetchSession()`**: لمزامنة الدور بعد الترقية أو عند فقدان المفتاح المحلي للدور، مع إمكانية مزامنة رمز FCM مع الخادم عند الحاجة.

### 5.8 الإشعارات في الجوال

- طلب أذونات الإشعارات عند التشغيل؛ الحصول على الرمز وحفظه محلياً.
- **الواجهة الأمامية:** عرض إشعارات من الخادم (قائمة، تعليم مقروء، تعليم الكل) مع سحب للتحديث حيث طُبّق.
- **النقر على إشعار FCM:** يمكن ربطه بتوجيه حسب `data.type` (مثل تذكير سجل يومي، زيارة إشرافية، نتيجة اختبار) — يعتمد على إكمال المسارات/المفاتيح في `MaterialApp` أو `go_router` حسب إعداد المشروع.

---

## 6. الإضافات التي تم تنفيذها (جلسات التطوير الأخيرة)

هذه نقاط تجمع **ما أُضيف** لدعم أدوار متعددة في التطبيق وربطها بالخادم:

1. **`App\Support\MobileAppRole`**  
   توحيد منطق «أي دور يُعاد للجوال» من مستخدم Spatie (Teacher / Examiner / CenterSupervisor).

2. **`AuthController` (API)**  
   - تقييد تسجيل الدخول بالأدوار الثلاثة.  
   - إرجاع `role` ضمن `user` في استجابة تسجيل الدخول.  
   - إضافة **`session`** لإرجاع المستخدم والدور للتوكن الحالي.  
   - تسمية التوكن `mobile-app`.

3. **وحدات تحكم API جديدة**  
   - `ExaminerApiController`: `me`, `tests`, `test-assignments`, `test-results`.  
   - `SupervisorApiController`: `me`, `centers`, `halaqahs`, `teachers`.

4. **`routes/api.php`**  
   مجموعات `examiner/*` و`supervisor/*` مع وسيط `role:Examiner` و`role:CenterSupervisor`.

5. **Flutter**  
   - تخزين الدور في `TokenStorage` (`saveRole` / `getRole`، ومسح الدور مع التوكن).  
   - ثوابت المسارات في `ApiConstants` (بما فيها `session` ومسارات المختبر والمشرف).  
   - `LoginResult` و`SessionUser` مع حقل `role`.  
   - `RoleHome` + صفحات `ExaminerHomePage` / `SupervisorHomePage` وقوائم البيانات.  
   - منطق الـ Splash في `main.dart` لاستدعاء `fetchSession` عند الحاجة.  
   - بعد تسجيل الدخول: الانتقال إلى `RoleHome(role: ...)` بدل افتراض «معلم فقط».

6. **الاختبارات والأدوات**  
   تحديث mocks (مثل Mockito) بعد توسيع `TokenStorage`، وتشغيل `build_runner` عند الحاجة.

7. **Firebase و FCM (الخلفية)**  
   حزمة Kreait، `config/firebase.php`، حقل `fcm_token` على `users`، `NotificationService`، فئات الإشعارات، ربط بعد حفظ نتيجة اختبار وزيارة مشرف، وأمر `notify:unrecorded-halaqahs` + الجدولة في `routes/console.php`.

8. **إشعارات داخل التطبيق (API)**  
   نموذج `AppNotification`، جدول `app_notifications`، `NotificationController`، ومسارات القراءة/التعليم كمقروء تحت Sanctum.

9. **Spatie Activity Log**  
   Migration وتكوين؛ تسجيل نشاط على نماذج رئيسية (مثل `TestResult` والزيارات الميدانية للمشرف)؛ صفحة Filament «سجل الأحداث» لـ SuperAdmin؛ جدولة تنظيف السجل.

10. **Filament — ويدجتات وتصدير**  
    ويدجتات لوحة إضافية (اختبارات، حضور، زيارات، غياب معلمين)؛ أزرار **تصدير Excel** في تقارير الحضور والاختبارات.

11. **Flutter**  
    تكامل Firebase Messaging والتخزين الآمن لـ FCM، مزامنة مع تسجيل الدخول/الجلسة، صفحة الإشعارات، زر الجرس في الصفحات الرئيسية للمعلم والمختبر والمشرف.

---

## 6.b العمل دون إنترنت والمزامنة المؤجلة (Offline-first)

التطبيق يعمل بلا شبكة ويزامن تلقائياً عند عودتها. الصور **خارج** النطاق: رفعها يبقى أونلاين فقط، فحمولات الطابور كلها JSON.

### الخادم

| العنصر | الوصف |
|--------|--------|
| **`config/offline.php`** | `sync_window_days` (7)، `evaluation_edit_hours` (1)، `max_client_clock_skew_minutes` (10). تُضبط عبر `.env`. |
| **`App\Support\OfflineSync`** | حدود نافذة التواريخ، تفسير الطابع الزمني القادم من الجهاز مع كبح انحراف الساعة، وبناء أعمدة التدقيق. |
| **Migration** | `client_recorded_at` + `synced_at` على `attendance_records`, `daily_evaluations`, `memorization_entries`, `test_results`؛ و`client_uuid` (فريد) + العمودين على `supervisor_field_visits`. |
| **`POST /api/teacher/daily-records/upsert`** | صار يقبل أي تاريخ ضمن النافذة بدل «اليوم فقط»، ويقبل `client_recorded_at` لكل سجل. |
| **`GET /api/teacher/halaqah/today?date=`** | صار يحترم معامل `date` ضمن النافذة (كان يتجاهله ويفرض اليوم دائماً) ليتحقق التطبيق مما وصل بعد المزامنة. |
| **`POST /api/supervisor/visits`** | صار **idempotent** عبر `client_uuid`: إعادة الإرسال تُعيد الزيارة نفسها (`was_created=false`, 200) بدل إنشاء زيارة مكررة وإشعار ثانٍ. |
| **`POST /api/examiner/test-results`** | `tested_at` يؤخذ من وقت الجهاز لا من `now()` (وإلا سُجّل وقت المزامنة)، والإشعار يُرسل عند الإنشاء أو تغيّر الدرجة فقط. |

**نافذة تعديل التقييم** تُحتسب الآن من `client_recorded_at` لا من `created_at`. هذا يحفظ حق المعلّم الذي عدّل خلال دقيقتين على جهازه ثم تأخرت الشبكة، ويمنع في الوقت نفسه اكتساب نافذة جديدة بمجرد تأخير المزامنة. والعمود يُكتب **عند الإنشاء فقط** فلا تُمدَّد النافذة بإعادة الإرسال.

### التطبيق (`mobile/lib/core/offline/`)

| الملف | الدور |
|-------|-------|
| `offline_database.dart` | SQLite: جدولا `outbox` و`cache_entries`. المصنع والمسار قابلان للحقن للاختبار على قاعدة في الذاكرة. |
| `outbox_dao.dart` | طابور الكتابة: إدراج ودمج (`dedupe_key`)، جدولة المحاولات، وسم الفشل، إعادة المحاولة. |
| `cache_dao.dart` | كاش استجابات القراءة (JSON خام + وقت الجلب). |
| `caching_api_client.dart` | يعترض كل `GET`: يخزّن الناجح، ويعيد المخزَّن عند انقطاع الشبكة. **كل** شاشات القراءة تعمل دون إنترنت بلا تعديل أي مستودع. |
| `connectivity_service.dart` | بثّ حالة الشبكة (مؤشر لبدء المزامنة، لا دليل على الوصول للخادم). |
| `sync_manager.dart` | تفريغ الطابور، تباعد أُسّي بسقف 30 دقيقة، وتصنيف الأخطاء. |
| `offline_gateway.dart` | قرار «أرسل الآن أم أدرج في الطابور»، وحقن `client_recorded_at`. |
| `widgets/` | شريط الحالة العلوي + شاشة «العمليات غير المرسلة». |

**تصنيف الأخطاء** هو القرار الأخطر في الطبقة:

- **مؤقت** (يُعاد) — انقطاع/مهلة (بلا `statusCode`)، 5xx، 408، 429، و**401** لأن إسقاط عمل المعلّم لمجرد انتهاء الجلسة غير مقبول.
- **نهائي** (يُوسَم للمستخدم) — بقية 4xx: تحقق، صلاحية، تاريخ خارج النافذة. إعادة محاولتها بلا نهاية تعني طابوراً عالقاً وبطارية مستنزفة.

**قواعد تصميم مقصودة:**

- الكاش يُستخدم عند **انقطاع الاتصال فقط**؛ رفض الخادم (403/422) جواب صحيح يجب أن يصل المستخدم لا أن يُخفى خلف نسخة قديمة.
- حفظ متكرر لنفس اليوم يتّحد في عملية واحدة بأحدث القيم، مع إبقاء وقت أول إدخال.
- عملية فشلت نهائياً **لا** تبتلعها محاولة حفظ جديدة؛ تبقى ظاهرة للمستخدم بسبب الرفض وخياري إعادة المحاولة أو الحذف الصريح.
- الطابور والكاش مفهرسان بمعرّف المستخدم (`TokenStorage.saveUserKey`)، وتسجيل الخروج **لا** يمحو الطابور — العمل غير المُرسل يُستعاد عند إعادة الدخول.
- على الويب (لا يدعم sqflite) يبقى التطبيق بسلوكه السابق: متصل فقط، دون انهيار.

### الاختبارات

- **الخادم:** `tests/Feature/OfflineSyncTest.php` — 13 اختباراً (نافذة التواريخ، ساعة الجهاز، عدم التكرار عند إعادة الإرسال، انتحال معرّف زيارة، وقت الاختبار الفعلي، عدم تكرار الإشعارات).
- **التطبيق:** `test/core/offline/` — 21 اختباراً (دمج الطابور، جدولة المحاولات، عزل المستخدمين، تصنيف الأخطاء، التباعد، صياغة الطابع الزمني، حالات شريط الحالة).

---

## 7. الصلاحيات في المزروع (Seeder) مقابل الجوال

ملف `RolesAndPermissionsSeeder` يعرّف صلاحيات **لوحة الويب** (مثل `view centers` لمشرف المركز). تطبيق الجوال **لا يقرأ هذه القائمة مباشرة**؛ يعتمد على:

- دور Spatie (`Teacher` / `Examiner` / `CenterSupervisor`) لمسار تسجيل الدخول و`MobileAppRole`.
- ما يُرجعه كل مسار API وسياسات النماذج (Policies) على الخادم.

تطبيق الجوال يعكس ما يوفّره `api.php`: المعلم يعدّل السجل اليومي وملفات الطلاب؛ المختبر يسجّل النتائج؛ المشرف يسجّل الزيارات ويطّلع على الحضور — بينما إدارة الكيانات الأساسية (مراكز، حلقات، مستخدمين…) تبقى في الغالب من Filament.

---

## 8. التشغيل السريع (مرجع)

**الخلفية** (من مجلد `backend/`):

- نسخ `.env` وضبط قاعدة البيانات، ثم `php artisan migrate --seed` حسب مشروعكم (يشمل جداول `fcm_token`، `app_notifications`، `activity_log` عند توفر الـ migrations).
- تعيين `FIREBASE_CREDENTIALS` أو وضع `firebase-credentials.json` تحت `storage/app/` للإنتاج.
- تفعيل الجدولة على الخادم: `php artisan schedule:run` عبر cron.
- `php artisan serve` — عادةً `http://127.0.0.1:8000`.

**التطبيق** (من `mobile/`):

- `flutter pub get`
- إعداد Firebase للمنصات (ملفات `google-services.json` / `GoogleService-Info.plist` وتشغيل `flutterfire configure` حسب الحاجة).
- `flutter run` (مع اختيار الجهاز أو Chrome للويب).

تأكد أن `ApiConstants.baseUrl` يشير إلى خادم Laravel الذي يعمل على جهازك أو شبكتك.

---

## 9. الاختبارات

- **Laravel:** مجموعة اختبارات Feature تغطي مسارات API والسياسات (حسب ما هو موجود تحت `backend/tests/`).
- **Flutter:** اختبارات وحدات/ويدجت تحت `mobile/test/`؛ بعد تغيير العقود يُفضَّل تشغيل `flutter test` و`dart run build_runner build` عند استخدام Mockito.

---

*آخر تحديث للتوثيق: نيسان (أبريل) 2026 — يشمل إشعارات FCM والإشعارات داخل التطبيق، الجدولة، ويدجتات Filament وتصدير Excel، وسجل النشاط، وتحديثات مسارات المختبر والمشرف في API والجوال كما في المستودع الحالي.*
