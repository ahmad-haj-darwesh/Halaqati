<?php

return [

    /*
    |--------------------------------------------------------------------------
    | نافذة المزامنة المؤجلة (بالأيام)
    |--------------------------------------------------------------------------
    |
    | Arabic: أقصى عدد أيام للخلف يقبل الخادم استلام سجلات يومية عنها من تطبيق
    | الجوال. الغرض هو ألّا يضيع عمل المعلّم الذي سجّل بلا إنترنت، مع إبقاء سقف
    | يمنع التلاعب بسجلات قديمة بعد اعتماد التقارير. القيمة 0 تعني «اليوم فقط»
    | (السلوك قبل دعم العمل دون إنترنت).
    | EN: How many days back the API accepts offline daily records.
    |
    */

    'sync_window_days' => (int) env('OFFLINE_SYNC_WINDOW_DAYS', 7),

    /*
    |--------------------------------------------------------------------------
    | نافذة تعديل التقييم (بالساعات)
    |--------------------------------------------------------------------------
    |
    | Arabic: المهلة المسموح فيها للمعلّم بتعديل تقييم يومي بعد أول إدخال. تُحتسب
    | من `client_recorded_at` عند وصول السجل من طابور المزامنة، ومن `created_at`
    | خلاف ذلك — حتى لا يُعاقَب معلّم عدّل خلال دقيقتين على جهازه ثم تأخرت الشبكة.
    | EN: Teacher evaluation edit window, measured from client time when synced offline.
    |
    */

    'evaluation_edit_hours' => (int) env('OFFLINE_EVALUATION_EDIT_HOURS', 1),

    /*
    |--------------------------------------------------------------------------
    | أقصى انحراف مقبول لساعة الجهاز (بالدقائق)
    |--------------------------------------------------------------------------
    |
    | Arabic: `client_recorded_at` يأتي من ساعة الجهاز وهي غير موثوقة. أي قيمة
    | في المستقبل بأكثر من هذا الحد تُهمَل ويُستخدم وقت الخادم بدلاً منها، حتى لا
    | يُطيل مستخدمٌ نافذةَ التعديل بتقديم ساعة جهازه.
    | EN: Clamp for untrusted client clocks; future timestamps beyond this are ignored.
    |
    */

    'max_client_clock_skew_minutes' => (int) env('OFFLINE_MAX_CLOCK_SKEW_MINUTES', 10),

];
